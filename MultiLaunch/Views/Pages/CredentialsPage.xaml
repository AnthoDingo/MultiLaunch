<Page 
    x:Class="MultiLaunch.Views.Pages.CredentialsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MultiLaunch.Views.Pages"
    xmlns:models="clr-namespace:MultiLaunch.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="CredentialsPage"
    d:DataContext="{d:DesignInstance local:CredentialsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    
    <StackPanel VerticalAlignment="Top">

        <ui:TextBlock Margin="0,24,0,0"  Text="Credentials" FontSize="24" FontWeight="Bold" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:Card Margin="0,24,0,0" Grid.Column="0">
                <StackPanel Margin="8,0,10,0">
                    <ComboBox Width="400" Margin="0,0,0,0" ItemsSource="{Binding ViewModel.CredentialTypes}" SelectedItem="{Binding ViewModel.SelectedCredentialType, Mode=TwoWay}" />
                    <Grid Margin="0,24,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Label Grid.Column="0" Grid.Row="0" Content="Username" Margin="0,8,0,0" Target="{Binding ElementName=tbxUsername}" />
                        <ui:TextBox x:Name="tbxUsername" Grid.Column="1" Grid.Row="0" Margin="16,0,0,0" Text="{Binding ViewModel.SelectedCredential.Username}" />

                        <Label Grid.Column="0" Grid.Row="1" Content="Password" Margin="0,16,0,0" Target="{Binding ElementName=tbxPassword}" />
                        <ui:PasswordBox x:Name="tbxPassword" Grid.Column="1" Grid.Row="1" Margin="16,8,0,0"  Text="{Binding ViewModel.MaskedPassword}"/>

                        <Label Grid.Column="0" Grid.Row="3" Content="Domain" Margin="0,16,0,0" Target="{Binding ElementName=tbxDomain}" />
                        <ui:TextBox x:Name="tbxDomain" Grid.Column="1" Grid.Row="3" Margin="16,8,0,0" Text="{Binding ViewModel.SelectedCredential.Domain}" />
                    </Grid>

                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:Button Grid.Column="1" Content="Save" Command="{Binding ViewModel.SaveCommand}" />
                    </Grid>
                </StackPanel>

            </ui:Card>

            <ui:ListView Grid.Column="1" ItemsSource="{Binding ViewModel.CustomCreds}">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:AppCred}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ui:TextBlock Grid.Column="0" Text="{Binding FQDN}" />
                            <ui:Button Grid.Column="1" Margin="16,0,0,0" Command="{Binding DataContext.DeleteCredentialCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" Content="Delete" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ui:ListView>
        </Grid>
    </StackPanel>
</Page>
